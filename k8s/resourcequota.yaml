apiVersion: v1
kind: ResourceQuota
metadata:
  name: demo_service-quota
  namespace: demo
  labels:
    app: demo_service
spec:
  hard:
    requests.cpu: 1
    requests.memory: 1Gi
    limits.cpu: 2
    limits.memory: 2Gi
    pods: "20"
